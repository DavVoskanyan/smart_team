import React from 'react';
import moduleStyles from './Header.module.scss';

const Header: React.FC = () => {
    return (
        <header className={moduleStyles.header}></header>
    );
};

export default Header;