import React from 'react';
import moduleStyles from './Sidebar.module.scss';

const Sidebar: React.FC = () => {
    return (
        <nav className={moduleStyles.nav}>

        </nav>
    );
};

export default Sidebar;