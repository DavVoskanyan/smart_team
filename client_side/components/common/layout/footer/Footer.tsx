import React from 'react';
import moduleStyles from './Footer.module.scss'

const Footer: React.FC = () => {
    return (
        <footer className={moduleStyles.footer}></footer>
    );
};

export default Footer;